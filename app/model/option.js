var Option = function(code, name, type, text, bg, icon, datasource, data)
{
    this.code = code;
    this.name = name;
    this.type = type;
    this.text = text;
    this.bg = bg;
    this.icon = icon;
    this.datasource = datasource;
    this.data = data;
}